export const API_BASE_URL = '/api'

export const SUPPORTED_FILE_TYPES = ['.txt', '.md', '.pdf', '.zip']

export const MAX_FILE_SIZE = 10 * 1024 * 1024 // 10MB

export const CHAT_CONFIG = {
  maxMessages: 100,
  streamingEnabled: true,
}